[project]
name = "daily-digest"
version = "0.1.0"
description = "Once-daily AI news digest from RSS feeds, published as a static page"
requires-python = ">=3.12"
dependencies = ["feedparser>=6.0", "google-genai>=1.0"]
